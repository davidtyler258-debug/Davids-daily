# Node (if build tools added later)
node_modules/
npm-debug.log*

# macOS
.DS_Store

# Windows
Thumbs.db
desktop.ini

# Environment / secrets — NEVER commit these
.env
.env.local
.dev.vars

# Cloudflare
.wrangler/
